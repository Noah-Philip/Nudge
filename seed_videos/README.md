Put 5-10 local `.mp4` files in this folder, then run:

`npm run seed`

If this folder is empty, the seed script will generate 6 tiny local MP4 demo clips automatically so the feed can still be demoed.
